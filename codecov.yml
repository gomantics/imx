coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 0%
    patch:
      default:
        target: 95%
        threshold: 0%

comment:
  layout: "diff, files"
  behavior: default
  require_changes: false

ignore:
  - "cmd/**"
  - "examples/**"
